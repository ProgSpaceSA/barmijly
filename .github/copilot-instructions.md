Follow `AGENTS.md` as the project operating manual.

Use `.cursor/skills/` (same files as `.claude/skills/`) for workflows: ticket changes, API+UI sync, Prisma migrations, debug, QA, and reviews.

Do not commit `.env` files. Do not hard-delete tickets. Do not skip RBAC.
